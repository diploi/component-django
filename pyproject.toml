[project]
name = "djangoapp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "Django==6.0.7",
    "environs==15.0.0",
]

[dependency-groups]
deploy = ["gunicorn==25.3.0",]
